# Quantum Vortex Master Plan v247

## Outcome

The New Eden reveal now behaves as one directed event rather than a collection
of unrelated effects. It uses the existing archive progress timeline and
preserves the approved map, portal canvases, audio director, interactions, and
crossing video.

## Choreography

1. **Dormant** — portal optics sleep at zero map progress.
2. **Compression** — the field tightens and the dark core anticipates.
3. **Singularity** — lensing, photon ring, and telemetry become legible.
4. **Rupture** — the shockwave expands while the horizon acquires lock.
5. **Locked** — the portal stabilizes around the authored New Eden map.
6. **Crossing** — the existing portal-enter state collapses the core and fires
   the threshold rupture before the 4K/120 FPS transition.

## Performance contract

- The director creates no `requestAnimationFrame` or interval.
- It consumes the archive's existing per-frame progress event.
- Frame cadence is measured with an exponential moving average.
- `ultra`, `high`, and `balanced` tiers preserve choreography while scaling
  secondary optical cost.
- Reduced-motion retains a composed still and removes expansion pulses.
- Hidden or inactive phases stop producing director work.

## Production assets

- `portal-transition-production.mp4`: 3840×1766, 120 FPS, 12.04 seconds,
  H.264 High profile, progressive download metadata.
- `yatagarasu-blueprint-quant.glb`: production default retaining the approved
  blueprint silhouette.
- Original masters remain preserved via Git LFS and are not on the default
  loading path.

## Validation

- Real activation and authentication flow reached the archive.
- Scroll-driven transition reached `data-qv-phase="locked"`.
- Runtime selected adaptive `balanced` tier under measured load.
- Telemetry reported `QUANTUM SHEAR // STABLE` and
  `EVENT HORIZON // LOCKED`.
- Browser console returned zero errors.
- Static vortex and production-readiness contracts pass.
